-- 주석
-- 한줄 주석밖에 없다.


-- 테이블 목록 확인
-- 4개의 가상 더미 데이터 확인해볼 수 있다. 
select tname from tab;

-- 사원정보 테이블(emp)
select * from emp;

-- 부서정보 테이블(dept)
select * from dept;

-- 테이블 : 세로줄(컬럼)과 가로줄(row, 레코드)의 모델을 이용하여 정렬된 데이터 집합(값)의 모임
-- 집합의 단위가 테이블
-- 테이블을 통해서 데이터를 관리




